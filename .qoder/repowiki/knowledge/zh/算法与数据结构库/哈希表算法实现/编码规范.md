- 所有哈希表实现均支持通过构造函数注入 `ToKeyStr`（键转字符串）和 `ToHashCode`（字符串转哈希码）函数，以实现自定义哈希逻辑。
- 在开放寻址法（线性/平方探测）的实现中，删除操作不直接移除元素，而是将其替换为全局常量 `EMPTY_KEY_VALUE` 以维持探测链的完整性。
- 每个具体实现类均维护一个受保护的 `_size` 字段，并在 `set` 和 `remove` 操作中同步更新，以支持 O(1) 复杂度的 `size` 属性访问。