- 所有堆操作均基于数组 `_list` 存储，并通过索引计算（`2*i+1`, `2*i+2`, `(i-1)/2`）维护二叉堆结构。
- 比较逻辑依赖外部注入的 `CompareFn` 及 `Compare` 枚举，而非硬编码比较运算符，以支持泛型类型。
- 公共修改操作（如 `add`, `pop`）在改变数组状态后自动触发相应的结构调整方法（`siftUp` 或 `siftDown`）以维持堆性质。