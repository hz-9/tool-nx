- 采用模板方法模式：抽象基类 `Heap` (`_base.heap.ts`) 定义核心流程（`add`, `pop`, `peek`）及辅助方法（`swap`, `getLeftIndex` 等），并将差异化的上浮（`siftUp`）和下浮（`siftDown`）逻辑声明为抽象方法。
- 具体实现：`MaxHeap` 和 `MinHeap` 分别继承基类，通过注入不同的比较逻辑（基于 `Compare.LESS_THAN` 或 `Compare.BIGGER_THAN`）实现最大堆和最小堆特性。
- 导出入口：`index.ts` 统一导出两种堆实现，屏蔽内部基类细节。