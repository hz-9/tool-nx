- 所有公共 API 均使用 JSDoc 注释，并同时提供中英文双语描述。
- 算法函数优先使用泛型 `<T>` 以支持任意数据类型，并在比较操作中提供默认的 `defaultEquals` 和 `defaultCompare` 实现。
- 集合操作类 `SetPlus` 采用继承原生 `Set` 的方式扩展功能，且集合运算方法（如 `union`, `intersection`）设计为静态方法并返回新的 `SetPlus` 实例。