- 所有树实现均遵循统一的泛型接口契约，通过注入 `CompareFn` 支持自定义比较逻辑，默认使用 `defaultCompare`。
- 核心递归操作（如插入、删除、遍历）均拆分为公共包装方法与受保护的 `_xxxNode` 递归辅助方法，以隔离外部调用与内部节点处理逻辑。
- 自平衡树（AVL/红黑树）在重写基类方法时，严格保持方法签名一致，并通过调用 `super` 或复用基类逻辑来减少代码冗余。