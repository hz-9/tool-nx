- 所有栈实现类均提供只读属性 `size` 和 `isEmpty` 以获取当前状态。
- 所有栈实现类均包含 `clear()` 方法用于重置栈状态，以及 `toString()` 和 `toArray()` 辅助方法用于数据转换与展示。
- 核心操作方法 `push`, `pop`, `peek` 在所有实现中保持统一的签名与语义，其中 `pop` 和 `peek` 在栈为空时返回 `undefined`。