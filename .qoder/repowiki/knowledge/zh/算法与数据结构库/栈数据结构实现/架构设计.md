- 抽象基类 `Stack<T>` (`_base.stack.ts`) 定义核心接口（`push`, `pop`, `peek`）并继承自通用 `Base<T>`。
- 具体实现类包括 `ArrayStack`（基于原生数组）、`LinkedListStack`（基于单向链表）和 `ObjectStack`（基于数字索引对象），均实现或继承自 `Stack<T>` 契约。
- `index.ts` 作为模块入口，统一导出所有具体实现类，对外提供一致的 API 访问。