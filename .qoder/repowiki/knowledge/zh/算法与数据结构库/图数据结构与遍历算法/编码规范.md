- 顶点与边的泛型封装：使用 `Vertice<T>` 和 `Edge<V>` 类包装原始数据，而非直接使用原始值或元组，以增强类型安全性和扩展性。
- 搜索回调标准化：BFS 和 DFS 遍历函数均接收统一的 `SearchCallback<T, Edge<Vertice<T>>>` 回调，用于在访问节点时执行自定义逻辑。
- 最短路径结果结构统一：BFS 和 DFS 的最短路径函数均返回包含 `distances`（距离映射）和 `prodecessors`（前驱节点映射）的标准接口对象。