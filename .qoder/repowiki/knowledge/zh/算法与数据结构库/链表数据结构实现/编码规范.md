- 所有链表类均使用泛型 `<T>` 以支持任意数据类型，并在基类中通过 `EqualsFn` 处理自定义相等性判断。
- 遵循头尾节点缓存模式，在基类中维护 `_headNode` 和 `_tailNode` 以优化首尾操作的时间复杂度。
- 操作方法成对出现，区分节点操作（如 `popNode`）与值操作（如 `pop`），前者返回节点对象，后者返回值。
- 在插入和删除操作中，严格维护前后指针（`next`/`prev`）的引用关系，确保链表结构的完整性。