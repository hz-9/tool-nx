- 所有具体队列类均实现 `size`、`isEmpty`、`first`、`last` 等 getter 属性以提供 O(1) 的状态访问。
- 每个队列类均提供 `toString()` 和 `toArray()` 方法，用于将队列内容转换为字符串表示或数组副本。
- 基于对象的实现（ObjectQueue/ObjectDeque）使用 `_flag` 索引标记队头位置，以避免数组移位带来的性能开销，并在队列为空时重置标志。