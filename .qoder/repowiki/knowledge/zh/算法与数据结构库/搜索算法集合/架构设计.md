- 入口文件 `index.ts` 统一导出所有搜索算法模块。
- 基础比较逻辑依赖上层 `_base` 模块提供的 `Compare` 枚举与 `defaultCompare` 等工具函数。
- 复杂搜索（如树形搜索）通过组合 `binary-tree` 模块中的数据结构（BST, AVL, RedBlack）实现，体现了算法与数据结构的解耦设计。
- 哈希搜索依赖 `hashmap` 模块的 `BetterHashmap` 实现，展示了跨模块复用能力。