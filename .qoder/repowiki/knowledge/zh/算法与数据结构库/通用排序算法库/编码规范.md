- 所有排序函数均使用泛型 `<T>` 定义，并接收可选的比较函数 `compareFu` 或数值转换函数 `toNumber`，默认值指向 `_base` 中的实现。
- 每个算法文件均通过 JSDoc 详细标注时间复杂度（平均、最好、最坏）和空间复杂度，并提供中英文双语说明。
- 遵循原地排序（In-place）优先的设计，大多数算法直接修改并返回原数组引用，仅归并排序等少数算法明确返回新数组。
- 文件名采用 `[algorithm-name].sort.ts` 的统一命名规范，导出函数名采用驼峰式 `[algorithmName]Sort`。